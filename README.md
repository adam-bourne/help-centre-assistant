# Steps to run workbook
1. Create a venv (add instructions for this)
2. install packages pip install -r requirements.txt
3. Create the chunks by running `python scripts/generate_chunks.py` from root dir